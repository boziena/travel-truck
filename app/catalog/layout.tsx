export const metadata = {
  title: "Catalog — TravelTrucks",
  description: "Browse and filter available campers for rent",
};

export default function CatalogLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
